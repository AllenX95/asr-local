{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Core Tauri permissions required by the main desktop window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
